<%if (ampStory && ampStory.fonts) { %>
    <% ampStory.fonts.forEach(font => { %>
        @font-face {
        font-family: <%=font.family%>;
        src: url('<%=font.path%>');
        font-weight: <%= font.weight %>;
        font-style: <%= font.style %>;
        }
    <% })%>
<% } %>
